// Package concerns is empty, and it stays empty.
//
// The assertions it would hold are methods on fluent.AssertableJSON, because a
// method's receiver must be declared in the same package as the method. They
// live in hesape/testing/fluent, grouped one file per subject: existence and
// size, values, the accounting, and debugging.
package concerns
