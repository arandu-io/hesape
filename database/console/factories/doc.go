// Package factories will hold make:factory, and holds nothing yet.
//
// A generator cannot be written before the thing it generates, so it belongs
// with database/model/factories and lands when that does.
package factories
