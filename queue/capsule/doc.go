// Package capsule is empty, and nothing is coming.
//
// A capsule lends a component to a program that did not build one: it wires the
// connections on the caller's behalf and hands back a manager. Outside an
// application the queue is the same queue.QueueManager, built the same way in
// the same three lines, so a capsule would be a second way to do what one line
// already does.
package capsule
