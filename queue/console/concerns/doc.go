// Package concerns is what more than one queue command needs.
//
// It holds [ParsesQueue], which splits a "connection:queue" argument into its
// two halves for the commands that take one.
package concerns
